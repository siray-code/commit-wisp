provider = "openai-compatible"
model = "gpt-4.1-mini"
base_url = "https://api.openai.com/v1"
language = "en"
format = "conventional"
max_input_tokens = 12000
candidates = 3
timeout_seconds = 30
# prompt_file = ".commit-wisp-prompt.txt"

# API keys do not belong in this file. Use `commit-wisp setup` or
# the COMMIT_WISP_API_KEY environment variable.
